import { defineComponent as I, ref as m, watch as A, onMounted as E, getCurrentInstance as O, unref as T, onBeforeUnmount as R, render as g, setBlockTracking as w, createElementVNode as D, mergeProps as S, renderSlot as _, h as C } from "vue";
const k = [
  "childRow",
  "column-sizing",
  "column-visibility",
  "destroy",
  "draw",
  "error",
  "init",
  "length",
  "order",
  "page",
  "preDraw",
  "preInit",
  "preXhr",
  "processing",
  "requestChild",
  "search",
  "stateLoadParams",
  "stateLoaded",
  "stateSaveParams",
  "xhr",
  "autoFill",
  "preAutoFill",
  "buttons-action",
  "buttons-processing",
  "column-reorder",
  "key",
  "key-blur",
  "key-focus",
  "key-refocus",
  "key-return-submit",
  "responsive-display",
  "responsive-resize",
  "rowgroup-datasrc",
  "pre-row-reorder",
  "row-reorder",
  "row-reordered",
  "dtsb-inserted",
  "deselect",
  "select",
  "select-blur",
  "selectItems",
  "selectStyle",
  "user-select",
  "stateRestore-change"
];
let v;
const L = {
  name: "Datatables.netVue",
  inheritAttrs: !1,
  use(i) {
    v = i;
  }
}, P = /* @__PURE__ */ I({
  ...L,
  props: {
    ajax: null,
    columns: null,
    data: null,
    options: null
  },
  emits: k,
  setup(i, { expose: f }) {
    const o = i, b = m(null), u = {}, a = m(), x = m([]);
    A(
      () => o.data,
      (t) => {
        let e = a.value;
        e && (c(e), e.clear(), e.rows.add(t).draw(!1));
      },
      {
        deep: !0
      }
    ), E(() => {
      var s, l;
      const t = O();
      let e = Object.assign({}, o.options) || {};
      if (o.data && (e.data = o.data, j(e.data)), o.columns && (e.columns = o.columns), e.columns && h(e.columns, t), o.ajax && (e.ajax = o.ajax), e.columnDefs || (e.columnDefs = []), t) {
        let r = Object.keys(t.slots);
        for (let n = 0; n < r.length; n++) {
          let d = r[n];
          if (d.match(/^column\-/)) {
            let p = d.replace("column-", "");
            e.columnDefs.push({
              target: p.match(/^\d+$/) ? parseInt(p) : p + ":name",
              render: "#" + d
            });
          }
        }
        h(e.columnDefs, t);
      }
      if (!v)
        throw new Error(
          "DataTables library not set. See https://datatables.net/tn/19 for details."
        );
      a.value = new v(T(b), e), (s = a.value) == null || s.on("preXhr", () => {
        c(a.value);
      }), (l = a.value) == null || l.on("column-reorder", () => {
        c(a.value);
      });
      for (let r of k)
        a.value && t && a.value.on(r, function() {
          var n = Array.from(arguments), d = n.shift();
          n.unshift({ event: d, dt: a }), n.unshift(r), t.emit.apply(t, n);
        });
    }), R(() => {
      var t;
      c(a.value), (t = a.value) == null || t.destroy(!0);
    });
    function j(t) {
      x.value = t.value ? t.value.slice() : t.slice();
    }
    function y(t) {
      return function(e, s, l, r) {
        let n = (r.settings.sTableId || r.settings.tableId) + "," + r.row + "," + r.col;
        if (!u[n]) {
          let d = C("div", t({
            cellData: e,
            colIndex: r.col,
            rowData: l,
            rowIndex: r.row,
            type: s
          }));
          u[n] = document.createElement("div"), g(d, u[n]);
        }
        return u[n];
      };
    }
    function h(t, e) {
      if (e)
        for (let l = 0; l < t.length; l++) {
          let r = t[l];
          if (typeof r.render == "string" && r.render.charAt(0) === "#") {
            var s = r.render.replace("#", "");
            e.slots[s] && (r.render = y(e.slots[s]));
          } else if (typeof r.render == "object" && typeof r.render.display == "string" && r.render.display.charAt(0) === "#") {
            var s = r.render.display.replace("#", "");
            e.slots[s] && (r.render.display = y(e.slots[s]));
          }
        }
    }
    function c(t) {
      let e = Object.keys(u), s = t.table().node().id;
      for (var l = 0; l < e.length; l++)
        e[l].indexOf(s + ",") === 0 && (g(null, u[e[l]]), delete u[e[l]]);
    }
    return f({
      dt: a
    }), (t, e) => e[0] || (w(-1), e[0] = D("div", { class: "datatable" }, [
      D("table", S({
        ref_key: "table",
        ref: b
      }, t.$attrs, { style: { width: "100%" } }), [
        _(t.$slots, "default")
      ], 16)
    ]), w(1), e[0]);
  }
}), $ = /* @__PURE__ */ (() => {
  const i = P;
  return i.install = (f) => {
    f.component("Datatables.netVue", i);
  }, i;
})();
export {
  P as DataTable,
  $ as default
};
